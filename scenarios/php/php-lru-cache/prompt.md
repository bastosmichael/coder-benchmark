
You are a PHP Expert.

Implement `LRUCache` in `src/LRUCache.php`.

```php
<?php

class LRUCache {
    // fields

    public function __construct($capacity) {
        
    }

    public function get($key) {
        // Return null if not found
    }

    public function put($key, $value) {
        
    }
}
```

Requirements:
1.  **O(1)**.
2.  Can use PHP Associative Arrays (ordered).
3.  **Strict**: Valid PHP syntax.

Output full content of `src/LRUCache.php` including `<?php` tag.
