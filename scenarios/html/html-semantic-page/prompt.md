
You are a Frontend Expert.

Create a semantic HTML5 landing page for a coffee shop "Brew Haven" in `src/index.html`.

Requirements:
1.  Structure: 
    *   `<!DOCTYPE html>`
    *   `<html>` with `lang` attribute.
    *   `<head>` with `<title>Brew Haven</title>` and `<meta>` charset.
    *   `<body>`
2.  Semantic Layout:
    *   `<header>` containing:
        *   `<h1>Brew Haven</h1>`
        *   `<nav>` with `<ul>` links (Home, Menu, Contact).
    *   `<main>` containing:
        *   `<section id="hero">`: Headline and CTA button.
        *   `<section id="features">`: 3 feature items (e.g., "Organic", "Fair Trade", "Cozy").
        *   `<section id="menu">`: A `<table>` listing at least 3 items (Name, Price).
    *   `<footer>`: Copyright text.
3.  **No CSS** required (just structure), but use semantic tags correctly.
4.  **No JavaScript** required.

Output full content of `src/index.html`.
